require(["gitbook", "jquery"], function(gitbook, $) {

	var load = function(){

        $('li.active').parents('li.chapter').addClass('expanded')
   	 };
	$(document).ready(load)

	gitbook.events.bind("page.change", load);
});